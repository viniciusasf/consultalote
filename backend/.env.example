# Copie para .env e preencha com os valores do seu projeto Supabase
# (Project Settings > API no painel do Supabase).
SUPABASE_URL=
SUPABASE_KEY=

# Usado só pelo script backend/scripts/migrate_sheets_to_supabase.py
SPREADSHEET_ID=1QDz2tiAKs_9YoAsFh1WMHj9UF8IyKhl4lifvdJLW6AM

# Autenticação (Master/Corretor). JWT_SECRET_KEY: qualquer string longa e
# aleatória (ex: python -c "import secrets; print(secrets.token_urlsafe(48))").
# MASTER_PASSWORD_HASH: gere com backend/scripts/hash_password.py.
JWT_SECRET_KEY=
MASTER_PASSWORD_HASH=
